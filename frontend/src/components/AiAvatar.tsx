export function AiAvatar() {
  return (
    <div
      className="shrink-0 flex items-center justify-center text-[10px] font-semibold tracking-widest select-none"
      style={{
        width: 22,
        height: 22,
        border: '1px solid var(--accent)',
        color: 'var(--accent)',
        background: 'var(--accent-low)',
      }}
    >
      AI
    </div>
  )
}
